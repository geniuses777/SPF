▶ 진행상황

[그래프 저장]

# 예측 결과 그래프 파일로 저장

[파이썬 실행 결과 db에 연동]

# pymysql 라이브러리 이용

# 예측 값을 저장할 수 있는 DB테이블 만들기

# DB테이블의 값 조회 (SELECT)

# DB테이블에 값 삽입 (INSERT)
  
   ----> O

# int형으로 변환한 후 예측 값 DB테이블에 삽입

# 이미지 파일 (BLOB타입  이미지 --> 바이너리) DB테이블에 삽입

# 오차율, 상승률(하락률)계산     # DB에 값 삽입

▶ 예정상황

# 파이썬 파일 주기적으로 실행